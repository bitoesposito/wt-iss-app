services:
  web:
    build:
      context: .
    ports:
      - '4200:4200'
    environment:
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true
    volumes:
      - .:/app
      - /app/node_modules
